//
//  Launch Apache.swift
//  easyMac
//
//  Created by Joseph May on 7/25/14.
//  Copyright (c) 2014 Joseph May. All rights reserved.
//

// The current version of this program no longer uses Apache, so this file doesn't really have a purpose at the moment

/*
import Foundation

//creates a task and runs LS
func createLS() {
    var task = NSTask()
    runLS(task)
}

//creates a task, sets the current directory to pathIN, and runs LS
func createCDLS(pathIn: String) {
    var task = NSTask()
    setCD(pathIn, task)
    runLS(task)
}


//TODO: make private
private func runLS(task: NSTask) {
    
    task.launchPath = "/bin/ls"
    task.arguments = ["-l"]
    
    task.launch()
    task.waitUntilExit()
    
}


//TODO: make private
private func setCD(pathIn: String, task: NSTask) {
    
  //  task.currentDirectoryPath = "\(task.currentDirectoryPath)/\(pathIn)"
    
}

func printPath() {
    var task = NSTask()
    println(task.currentDirectoryPath)
}
*/
